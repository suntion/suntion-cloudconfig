server:
    port: 9002
    ssl:
        key-store: classpath:suntion.cc.pfx
        key-store-type: PKCS12
        key-password: cgTv99ha
spring:
    application:
        name: suntion-gateway
    cloud:
        gateway:
            routes:
            -   id: suntion-sms
                uri: lb://suntion-sms
                predicates:
                - Path=/sms/**
            -   id: suntion-auth
                uri: lb://suntion-auth
                predicates:
                - Path=/auth/**
            discovery:
                locator:
                    lowerCaseServiceId: true
eureka:
    client:
        service-url:
            defaultZone: https://localhost:8761/eureka
